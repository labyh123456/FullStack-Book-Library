<!-- all authors  -->
<h2 class="page-header">Search Authors</h2>
<form action="/authors" method="GET">
<label>Name</label>
<input type="text" name="name" value="<%=searchOption.name %> ">
<button type="submit">Search</button>
</form> 

<br>
<% author.forEach(authors => { %>
<div> <%= authors.name  %></div>
<div class="btn-row">
<a class="btn btn-primary" href="/authors/<%= authors.id %>">View</a>
<a class="btn btn-primary" href="/authors/<%= authors.id %>/edit">Edit</a>
<%- include('../partials/deleteForm', {url: `/authors/${authors.id}` }) %>
</div>
<% }) %>
